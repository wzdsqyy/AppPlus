<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_text_line_helper"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:orientation="vertical"
    tools:context="com.wzdsqyy.applibDemo.text.TextLineHelperActivity">
    <TextView
    android:id="@+id/tv_content"
    android:layout_width="match_parent"
    android:maxLines="3"
    android:text="当我们使用 gradle dependencies 命令时，它输出的是根项目的依赖情况。如果我们的gradle项目没有子项目，使用它就够了。
但如果有子项目，情况就比较麻烦，因为gradle并没有提供一个命令，让我们一下子查看全部项目的依赖（我感觉这跟它的输出量实在太大有关）
目前没有特别简单的做法，只能靠手动，或者写一些task。\n       当我们使用 gradle dependencies 命令时，它输出的是根项目的依赖情况。如果我们的gradle项目没有子项目，使用它就够了。
但如果有子项目，情况就比较麻烦，因为gradle并没有提供一个命令，让我们一下子查看全部项目的依赖（我感觉这跟它的输出量实在太大有关）
目前没有特别简单的做法，只能靠手动，或者写一些task。\n       当我们使用 gradle dependencies 命令时，它输出的是根项目的依赖情况。如果我们的gradle项目没有子项目，使用它就够了。
但如果有子项目，情况就比较麻烦，因为gradle并没有提供一个命令，让我们一下子查看全部项目的依赖（我感觉这跟它的输出量实在太大有关）
目前没有特别简单的做法，只能靠手动，或者写一些task"
    android:layout_height="wrap_content" />

    <!--<TextView-->
        <!--android:id="@+id/tv_content"-->
        <!--android:layout_width="match_parent"-->
        <!--android:maxLines="3"-->
        <!--android:text="当我们使用"-->
        <!--android:layout_height="wrap_content" />-->
    <TextView
        android:id="@+id/tv_ex"
        android:layout_gravity="right"
        android:layout_width="wrap_content"
        android:textColor="#000"
        android:text="展开"
        android:layout_height="wrap_content" />

</LinearLayout>
